spring:
  application:
    name: ludotheque

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=LUDOTHEQUE_BD;encrypt=true;trustServerCertificate=true
    username: LUDO_USER
    password: Ludo_Pwd2026!

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true
